// Створити функцію яка приймає 2 параметра числа. Функція буде виводити найменше число серед параметрів
function Fox(age, age2) {
    if (age > age2) {
        console.log(age2);
    }
    else {
        console.log(age);
    }
}
Fox(3, 7);
